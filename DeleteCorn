local function eliminarCorn()
    for _, objeto in ipairs(workspace:GetDescendants()) do
        if objeto:IsA("Model") and objeto.Name == "Corn" then
            objeto:Destroy()
        end
    end
end

-- Ejecutar la función
eliminarCorn()
